#include <bits/stdc++.h>
using namespace std;

void insert(vector<int> arr, int addNumebr_)
{
    for (int i = 0; i < arr.size(); i++)
    {
        if (arr[i] >)
    }
}

int main()
{
    vector<int> arr = {1, 4, 6, 7, 9};
    int addNumebr = 5;
}