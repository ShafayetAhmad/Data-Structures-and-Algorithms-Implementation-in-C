#include <stdio.h>

void merge_sort(int arr[], int n)
{
    for (int i = 0; i < n; i *= 2)
    {
        /* code */
    }
}

int main()
{
    int arr[10] = {5, 8, 2, 3, 1, 9, 4, 7, 6, 0};
    merge_sort(arr, 10);
    return 0;
}